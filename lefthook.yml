pre-commit:
  commands:
    pre-commit:
      interactive: true
      run: mise run pre-commit
    poetry-check:
      run: poetry check
    poetry-export:
      run: poetry export
    poetry-lock:
      run: poetry lock